[project]
name = "openpi-inference-recorder"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0.0",
    "numpy>=2.5.2",
    "ml_dtypes>=0.6.0",
]

[project.optional-dependencies]
zstd = ["zstandard>=0.22.0"]

[tool.setuptools.packages.find]
include = ["recorder*"]
