record:
  enabled: true
  add_timestamp: true

hooks:
  enabled:
    - observation_input
    - token_spans
    - prefix_final_hidden_state
    - action_chunks
    - raw_attention_weights
    - value_vectors

  action_chunks:
    num_chunks: 1

  raw_attention_weights:
    layers: null

  value_vectors:
    layers: null